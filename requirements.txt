# ===========================================================================
# Dependencies for Legal RAG Chatbot (Nghị định 116/2020/NĐ-CP)
# ===========================================================================
#
# Quick setup in the 'chatbot' Conda environment:
#
#   conda activate chatbot
#
# ---------------------------------------------------------------------------
# 1. PyTorch Installation (Choose GPU CUDA or CPU)
# ---------------------------------------------------------------------------
# If you have an NVIDIA GPU (e.g. RTX 3050, RTX 40xx):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# If running CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# ---------------------------------------------------------------------------
# 2. Project Dependencies
# ---------------------------------------------------------------------------
# Install all required packages:
#   pip install -r requirements.txt
#
# ---------------------------------------------------------------------------

# Database: PostgreSQL driver for data import and indexing scripts
psycopg[binary]>=3.1.0

# Vector database adapter: pgvector extension support for PostgreSQL
pgvector>=0.3.0

# Embedding model: Official BAAI library for BAAI/bge-m3 dense vector generation
FlagEmbedding>=1.2.9

# Embedding models: Sentence Transformers for vnlegal-lal, vietlegal-harrier,
# vietlegal-e5, and jina-embeddings-v3
sentence-transformers>=3.0.0

# DeepX Embedding v1: Official deepx_embed package (Gated DeltaNet-2 architecture,
# NOT compatible with generic SentenceTransformer loading).
# Install: pip install git+https://github.com/dx-tech-ai/deepx-embed.git
# Requires: torch, transformers, huggingface_hub, einops (pulled automatically)
# Optional (faster inference, required for fine-tuning):
#   pip install git+https://github.com/fla-org/flash-linear-attention.git
# The deepx_embed package is listed separately because it is sourced from
# GitHub rather than PyPI and must be installed with the git+ URL.
# deepx-embed>=0.1.0  # uncomment and use pip install git+https://github.com/dx-tech-ai/deepx-embed.git

# BM25 retrieval: Okapi BM25 implementation (rank_bm25.BM25Okapi)
# Used by scripts/bm25_retriever.py as the standalone lexical retriever.
rank-bm25>=0.2.2

# Numerical arrays: required by rank-bm25 for score arrays (get_scores returns ndarray)
numpy>=1.24.0

# Testing: Unit test suite (tests/test_embedding.py, tests/test_retrieval.py,
# tests/test_bm25_retriever.py)
pytest>=8.0.0

